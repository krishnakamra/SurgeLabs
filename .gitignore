/node_modules
/.next/
/out/
/build
.DS_Store
*.pem
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.env*
!.env.example
.vercel
*.tsbuildinfo
next-env.d.ts

# Local quote log (dev storage fallback)
.data/
.env.local

# Working directory for scripts/fetch-media.mjs.
.media-cache/

# The rendered ad previews are committed (see ads/), but the scratch HTML is not.
ads/preview.html

# Rendered card proofs. Regenerate with `node scripts/make-card.mjs` —
# press-resolution PNGs do not belong in git history.
cards/
